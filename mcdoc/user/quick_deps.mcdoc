// Quickly generates a 'dependencies' array based on the world's currently installed/enabled packs.

use super::super::def::PackId

struct In {
    packs: [PackId]
}

enum(int) Return {
    Success = 1,
    InvalidArgs = 0,
}