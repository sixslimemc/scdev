// Command-line friendly wrapper around `slimecore:rebuild`.

use super::super::super::def::PackId

struct In {
    args: struct Args {
        uninstall?: [PackId],
        disable?: [PackId],
        enable?: [PackId],
        wipe_memory?: boolean,
        ignore_warnings?: boolean,
    }
}